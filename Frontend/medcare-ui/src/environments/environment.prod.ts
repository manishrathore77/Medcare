export const environment = {
  production: true,
  /**
   * Same-origin `/api` when you put a reverse proxy in front of UI + API.
   * If the API is on another host, set the full base URL here (e.g. https://api.example.com).
   */
  apiUrl: '',
  apiBackendOrigin: '',
};
